import 'package:get/get.dart';
import 'package:secured_calling/features/meeting/controllers/meeting_detail_controller.dart';

class MeetingDetailBinding extends Bindings {
  @override
  void dependencies() {
    // The controller will be created when the page is accessed
    // and will be automatically disposed when the page is closed
  }
}
